## Packages
(none needed)

## Notes
- Application runs in dark mode by default for a clean, minimal dashboard aesthetic.
- The `lastCheckedAt` timestamp is returned as an ISO string from the API and parsed via standard `Date` constructor on the client.
- Uses existing Shadcn UI components provided in the workspace.
